version: 2

build:
  os: ubuntu-24.04
  apt_packages:
    - libsodium-dev
  tools:
    python: "3.13"

sphinx:
  configuration: docs/conf.py
  builder: dirhtml

python:
  install:
    - method: pip
      path: .
    - requirements: docs/requirements.txt
