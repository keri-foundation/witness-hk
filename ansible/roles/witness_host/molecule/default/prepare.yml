---
- name: Prepare witness_host Molecule instance
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: Refresh apt package cache for Debian-based images
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 3600
      when: ansible_facts.os_family == 'Debian'