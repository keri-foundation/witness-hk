version: 2

build:
  os: ubuntu-24.04
  apt_packages:
    - libsodium-dev
  tools:
    python: "3.14"
  jobs:
    post_install:
      - python -m pip install --no-deps -e .

sphinx:
  configuration: docs/conf.py
  builder: dirhtml

python:
  install:
    - requirements: docs/requirements.txt
