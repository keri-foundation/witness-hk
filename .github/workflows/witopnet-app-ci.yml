# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions


name: GitHub Actions for Witopnet
on:
  push:
    branches:
      - 'main'
  pull_request:
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.14.0
        uses: actions/setup-python@v5
        with:
          python-version: "3.14.0"

      - name: Install dependencies
        run: |
          python -m pip install uv==0.9.18
          uv sync --locked --extra dev

      - name: Check formatting with black
        run: |
          uv run --locked --extra dev black --check src tests

      - name: Lint with ruff
        run: |
          uv run --locked --extra dev ruff check .

  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ macos-15, ubuntu-latest ]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.14.0
        uses: actions/setup-python@v5
        with:
          python-version: "3.14.0"

      - name: Install libsodium (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install libsodium
          echo "DYLD_LIBRARY_PATH=$(brew --prefix libsodium)/lib:$DYLD_LIBRARY_PATH" >> $GITHUB_ENV

      - name: Install dependencies
        run: |
          python -m pip install uv==0.9.18
          uv sync --locked --extra dev

      - name: Run tests
        run: |
          uv run --locked --extra dev pytest tests/
